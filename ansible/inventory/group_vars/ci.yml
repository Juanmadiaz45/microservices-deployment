---
# Jenkins configuration
jenkins_admin_username: "admin"
jenkins_url: "http://localhost:8080"
jenkins_admin_password: "{{ lookup('file', 'secrets.yml') | from_yaml | attr('jenkins_admin_password') }}"

# GitHub configuration
github_owner: "MGMaikeru"
github_repo: "microservice-app-example"
github_token: "{{ lookup('file', 'secrets.yml') | from_yaml | attr('github_token') }}"

# SonarQube configuration
sonarqube_url: "http://localhost:9000"
sonar_token: "{{ lookup('file', 'secrets.yml') | from_yaml | attr('sonarqube_token') }}"
sonar_autogenerated_token: "{{ lookup('file', 'secrets.yml') | from_yaml | attr('sonarqube_token') }}"

azure_vm_password: "Password@123"
docker_hub_username: "juanmadiaz45"
docker_hub_token: "{{ lookup('file', '../inventory/secrets.yml') | from_yaml | attr('docker_token') }}"
sonar_host_url: "http://51.137.206.160:9000"
